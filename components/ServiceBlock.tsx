import { ImageReveal } from './ImageReveal';
import { MagneticButton } from './MagneticButton';
import { RevealText } from './RevealText';
export function ServiceBlock({service,index}:{service:{title:string;eyebrow:string;text:string;items:string[]};index:number}){return <section className="luxury-container grid gap-10 border-t border-cream/10 py-16 md:grid-cols-2 md:items-center"><div className={index%2?'md:order-2':''}><RevealText><p className="mb-5 text-xs uppercase tracking-[.32em] text-sand">{service.eyebrow}</p><h3 className="font-display text-[clamp(3rem,7vw,7rem)] leading-[.84] tracking-luxury">{service.title}</h3><p className="mt-8 max-w-xl text-lg leading-8 muted">{service.text}</p><div className="mt-8 flex flex-wrap gap-3">{service.items.map(i=><span key={i} className="rounded-full border border-cream/12 px-4 py-2 text-sm text-cream/72">{i}</span>)}</div><div className="mt-10"><MagneticButton href="/kontakt">Projekt anfragen</MagneticButton></div></RevealText></div><ImageReveal label={service.title}/></section>}
