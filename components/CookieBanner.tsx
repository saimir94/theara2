'use client';
import { useEffect, useState } from 'react';
export function CookieBanner(){const [show,setShow]=useState(false); useEffect(()=>setShow(!localStorage.getItem('theara-cookie-consent')),[]); if(!show)return null; const save=(v:string)=>{localStorage.setItem('theara-cookie-consent',v);setShow(false)}; return <div className="fixed bottom-4 left-1/2 z-[100] w-[calc(100%-32px)] max-w-3xl -translate-x-1/2 rounded-[1.5rem] border border-cream/12 bg-ink/92 p-5 shadow-editorial backdrop-blur-xl"><p className="text-sm leading-6 muted">Wir verwenden notwendige lokale Einstellungen und optionale Komfort-Cookies. Sie können zustimmen, ablehnen oder Einstellungen lokal speichern.</p><div className="mt-4 flex flex-wrap gap-3"><button onClick={()=>save('accepted')} className="rounded-full bg-cream px-5 py-3 text-xs uppercase tracking-[.2em] text-ink">Akzeptieren</button><button onClick={()=>save('rejected')} className="rounded-full border border-cream/15 px-5 py-3 text-xs uppercase tracking-[.2em]">Ablehnen</button><button onClick={()=>save('settings')} className="rounded-full border border-cream/15 px-5 py-3 text-xs uppercase tracking-[.2em]">Einstellungen</button></div></div>}
